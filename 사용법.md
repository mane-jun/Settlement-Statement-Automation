# 정산내역서 자동화 - 사용법

프로그램은 **`정산내역서 자동화.bat`** 하나입니다. 여기서 모든 기능을 씁니다.

## 1. 쓰는 법

### 방법 A — 끌어다 놓기 (가장 빠름)

주문 엑셀 파일(또는 그 파일들이 든 폴더)을 **`정산내역서 자동화.bat` 아이콘 위로 끌어다 놓으면** 끝입니다.

- 폴더를 통째로 올리면 폴더 안 `.xlsx` 파일을 이름순으로 모두 처리합니다.
  (예: `260731_토바제작분` 폴더를 올리면 27일 ~ 31일 5개 파일을 한 번에)
- 파일 여러 개를 한꺼번에 선택해서 올려도 됩니다.

### 방법 B — 더블클릭해서 메뉴 고르기

```
--------------------------------------------
  1. 주문 파일 추가하기
  2. 주문 파일 추가하기 (중복 무시)
  3. 보관박스 금액 다시 계산하기
  4. 금액 설정 바꾸기
  5. 정산내역서 파일 바꾸기
  0. 끝내기
--------------------------------------------
```

한 번 고르면 실행하고 다시 메뉴로 돌아옵니다. 끝낼 때 `0`.

> 정산내역서 파일이 **엑셀에서 열려 있으면 저장을 못 합니다.** 닫고 실행해주세요.

## 2. 정산내역서 파일은 어떻게 찾나요

아래 순서로 자동으로 찾고, **한 번 정하면 기억해 둡니다** (`정산내역서_경로.txt`).

1. 프로그램이 있는 폴더
2. 지난번에 쓴 경로
3. 바로 위 폴더 (예: 바탕화면)
4. 그래도 없으면 파일 선택창

다른 파일로 바꾸려면 메뉴 `5번`을 쓰세요.

## 3. 옮기는 규칙

| 주문 파일 | → | 정산내역서 |
|---|---|---|
| 주문일 | → | **A열 주문일** (그 파일의 행 전체를 세로 병합) |
| — | | B열 구분 (비움) |
| 품목 | → | **C열 품목** |
| 수령인 | → | **D열 수령자** |
| 사이즈 | → | **E열 사이즈** (`152X203` → `152x203` 소문자로 통일) |
| — | | F열 단가 (비움) |
| 수량 | → | **G열 수량** |
| — | | H열 공급가액 · I열 VAT · J열 택배비 · K열 합계 (비움) |
| 보관박스 | → | **L열 보관박스** (금액으로 환산 → 4번) |
| — | | M열 비고 (비움) |

- 연락처 · 주소 · 배송메모 · 제작메모 · 순서는 옮기지 않습니다.
- 표의 맨 아래(품목이 적힌 마지막 행 다음)에 이어붙이고, 바로 윗 행의 서식을 그대로 물려받습니다.
- 인쇄 영역이 새로 추가한 행보다 짧으면 자동으로 늘려줍니다.
- 시트는 이름에 `년`·`월`이 들어간 첫 번째 시트(`26년 8월`)를 씁니다.

## 4. 보관박스 금액 규칙

주문 파일에 **보관박스 표시가 있는 행만** 금액이 들어갑니다. 표시가 없으면 빈칸입니다.

| 사이즈 | 단가 |
|---|---|
| 가로·세로가 **둘 다 254 미만** | **3,850원** |
| 가로·세로 중 **하나라도 254 이상** | **4,400원** |

여기에 **수량을 곱한 값**이 L열에 들어갑니다.

```
152x203  수량 1  ->  3,850
305x305  수량 2  ->  8,800   (4,400 x 2)
152x203  수량 3  -> 11,550   (3,850 x 3)
203x305  수량 1  ->  4,400   (세로가 305 이므로 큰 금액)
254x254  수량 1  ->  4,400   (254 는 "이상" 이므로 큰 금액)
```

> 긴 변 하나만 254 이상이어도 큰 금액입니다. 포장가방에 물건이 들어가야 하니 긴 쪽을 기준으로 잡았습니다.
> 면적 기준으로 바꾸려면 말씀해주세요.

### 금액·기준 바꾸기

메뉴 `4번` 을 고르면 메모장이 열립니다. 숫자만 고치고 저장하면 바로 적용됩니다.
(`보관박스_금액설정.txt` 를 직접 열어 고쳐도 됩니다)

```
기준사이즈 = 254
작은금액 = 3850
큰금액 = 4400
수량곱하기 = 예
기존글자도변환 = 예
```

### 글자로 남아 있는 "포장가방" 바꾸기

정산내역서에 글자로 `포장가방` 이라고 남아 있는 칸은 메뉴 `3번` 으로 금액으로 바꿉니다.
그 행의 사이즈·수량을 보고 계산합니다.

- 이미 **숫자가 들어 있는 칸은 건드리지 않습니다.**
- 주문을 추가할 때도 매번 자동으로 같이 돌아갑니다.
  (원치 않으면 설정에서 `기존글자도변환 = 아니오`)

## 5. 안전장치

- **자동 백업** — `백업` 폴더에 실행 직전 상태를 날짜·시각을 붙여 남깁니다.
  잘못됐으면 그 파일을 꺼내 쓰면 됩니다.
  바뀐 내용이 없으면 백업을 남기지 않아서 폴더가 쓸데없이 쌓이지 않습니다.
- **중복 방지** — 이미 들어가 있는 주문일은 건너뜁니다.
  같은 날짜를 일부러 한 번 더 넣어야 하면 메뉴 `2번`.

## 6. 파일 구성

| 파일 | 설명 |
|---|---|
| `정산내역서 자동화.bat` | **이것만 쓰면 됩니다** (끌어다 놓기 / 더블클릭) |
| `settlement.ps1` | 실제 동작하는 코드 |
| `보관박스_금액설정.txt` | 금액·기준 숫자 |
| `정산내역서_경로.txt` | 마지막으로 쓴 정산내역서 위치 (자동 생성) |
| `백업\` | 실행 결과를 되돌릴 수 있는 백업 |

설치할 프로그램은 없습니다. 윈도우 + 엑셀만 있으면 됩니다.
