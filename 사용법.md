# 정산내역서 자동화 - 사용법

프로그램은 **`정산내역서 자동화.bat`** 하나입니다. 여기서 모든 기능을 씁니다.

## 1. 쓰는 법

### 방법 A — 끌어다 놓기 (가장 빠름)

주문 엑셀 파일(또는 그 파일들이 든 폴더)을 **`정산내역서 자동화.bat` 아이콘 위로 끌어다 놓으면** 끝입니다.

- 폴더를 통째로 올리면 폴더 안 `.xlsx` 파일을 이름순으로 모두 처리합니다.
  (예: `260731_토바제작분` 폴더를 올리면 27일 ~ 31일 5개 파일을 한 번에)
- 파일 여러 개를 한꺼번에 선택해서 올려도 됩니다.

### 방법 B — 더블클릭해서 메뉴 고르기

```
--------------------------------------------
  1. 주문 파일 추가하기
  2. 주문 파일 추가하기 (중복 무시)
  3. 보관박스 금액 다시 계산하기
  4. 보관박스 금액 설정 바꾸기
  5. 단가표 고치기
  6. 택배비 설정 바꾸기
  7. 정산내역서 파일 바꾸기
  0. 끝내기
--------------------------------------------
```

한 번 고르면 실행하고 다시 메뉴로 돌아옵니다. 끝낼 때 `0`.

> 정산내역서 파일이 **엑셀에서 열려 있으면 저장을 못 합니다.** 닫고 실행해주세요.

## 2. 정산내역서 파일은 어떻게 찾나요

아래 순서로 자동으로 찾고, **한 번 정하면 기억해 둡니다** (`정산내역서_경로.txt`).

1. 프로그램이 있는 폴더
2. 지난번에 쓴 경로
3. 바로 위 폴더 (예: 바탕화면)
4. 그래도 없으면 파일 선택창

다른 파일로 바꾸려면 메뉴 `7번`을 쓰세요.

## 3. 옮기는 규칙

표는 **B열에서 시작합니다.** A열은 비워 둡니다.

| 주문 파일 | → | 정산내역서 | 어떻게 |
|---|---|---|---|
| 주문일 | → | **B열 주문일** | 그 파일의 행 전체를 세로 병합 |
| — | | C열 구분 | 비움 |
| 품목 | → | **D열 품목** | 그대로 |
| 수령인 | → | **E열 수령자** | 그대로 |
| 사이즈 | → | **F열 사이즈** | `152X203` → `152x203` 소문자로 통일 |
| — | | **G열 단가** | 단가표에서 찾아 넣음 (→ 4번) |
| 수량 | → | **H열 수량** | 그대로 |
| — | | **I열 공급가액** | 수식 `=G*H` |
| — | | **J열 VAT** | 수식 `=I*0.1` |
| — | | **K열 택배비** | 규칙으로 계산 (→ 5번) |
| — | | **L열 합계** | 수식 `=I+J+K+M` |
| 보관박스 | → | **M열 보관박스** | 금액으로 환산 (→ 6번) |
| — | | **N열 비고** | `단가확인`·`택배비확인`·`제주` 같은 표시를 남김 |

- 연락처 · 제작메모 · 순서는 옮기지 않습니다.
  (주소와 배송메모는 옮기지 않지만 **택배비를 정할 때 읽습니다.**)
- 표의 맨 아래(품목이 적힌 마지막 행 다음)에 이어붙이고, 바로 윗 행의 서식을 그대로 물려받습니다.
- 인쇄 영역이 새로 추가한 행보다 짧으면 자동으로 늘려줍니다.
- 시트는 이름에 `년`·`월`이 들어간 첫 번째 시트(`26년 8월`)를 씁니다.

> **I·J·L열은 값이 아니라 수식으로 들어갑니다.** 그래서 나중에 단가나 택배비를 손으로
> 고치면 공급가액·VAT·합계가 저절로 따라옵니다.

## 4. 단가 규칙

품목과 사이즈로 **`단가표.txt`** 를 찾아 G열에 넣습니다. 26년 1~7월 정산내역서 738행에서 뽑아낸 66개 규격이 들어 있습니다.

```
아크릴 | 152x203 | 4000
캔버스 | 203x254x30 | 10500
알루미늄 | 457x457 | 20000
```

- **품목**은 6가지로 묶어서 알아봅니다 — 아크릴 / 캔버스 / 알루미늄 / 원목클래식 / 호두나무 / 종이포스터
  `알루미늄골드(아크릴판)`, `알루미늄 화이트 프레임` 처럼 적혀 있어도 모두 **알루미늄** 한 줄로 봅니다.
  (골드·화이트·블랙·실버는 값이 같습니다)
- **사이즈**는 `짧은변x긴변` 으로 적습니다. 가로세로 순서, 대소문자, 구분자(`x X * _ 공백`)는 알아서 맞춥니다.
  `A2`·`A3`·`A4`·`A5` 표기도 알아봅니다.
  **±5mm 오차는 같은 규격으로 봅니다** (`202`와 `203`, `304`와 `305`는 같은 칸).
- **캔버스만 두께가 값을 바꿉니다.** `305x406x30`은 13,200원, `305x406x45`는 14,500원.
  두께를 안 적으면 30mm로 봅니다.
- 네 번째 칸에 **적용시작일**을 적으면 그 날짜부터 적용됩니다. 비워 두면 항상 적용됩니다.
  ```
  알루미늄 | 297x420 | 14400
  알루미늄 | 297x420 | 13900 | 2026-01-01
  ```
  (A3 알루미늄은 2026년부터 13,900원으로 내렸습니다)

> **표에 없는 사이즈는 단가를 비워 두고 비고에 `단가확인` 을 남깁니다.**
> 틀린 금액이 조용히 들어가는 것보다 눈에 띄게 비워 두는 쪽이 안전합니다.
> 단가만 손으로 채우면 공급가액·VAT·합계는 수식이라 저절로 계산됩니다.

바꾸려면 메뉴 `5번`.

## 5. 택배비 규칙

**긴변 + 짧은변의 합**으로 정합니다. 면적이나 긴 변 하나만 보는 게 아닙니다.

| 긴변+짧은변 | 택배비 | 해당 규격 (예) |
|---|---|---|
| ≤ 700 | **3,000원** | 152x203 · 203x305 · 254x254 · 305x305 |
| 701 ~ 900 | **3,500원** | 355x355 · 305x406 · A3 · 305x430 · 406x406 |
| 901 ~ 1,100 | **4,000원** | 457x457 · 406x508 · 508x508 · A2 |
| 1,101 ~ 1,300 | **5,000원** | 508x610 · 610x610 · 508x762 |
| 1,300 초과 | **6,000원** | 610x762 · 594x840 |

여기에 아래가 덧붙습니다.

- **수령인이 비어 있는 줄 → 0원.** 한 사람이 여러 개를 주문하면 첫 줄에만 수령인을 쓰고
  아래는 비워 두시죠. 배송이 한 번이니 택배비도 한 번만 붙습니다.
  (**보관박스는 줄마다 따로 붙습니다.** 가방은 개수대로 드니까요)
- **배송메모에 `방문`·`방문수령`·`퀵` → 0원**
- **주소가 제주 → 기본 택배비 + 3,000원**
- **종이포스터 → 한 단계 아래 금액** (말아서 보내니 가볍습니다)
- **수량이 2개 이상이면 비워 둡니다.** 상자를 몇 개 쓸지 그때그때 달라서 규칙이 서지 않았습니다.
  비고에 `택배비확인` 을 남기니 그 줄만 직접 넣으시면 됩니다.
  (1~7월 기준 전체의 82%가 수량 1개라, 손이 가는 줄은 다섯에 하나 꼴입니다)

구간·금액은 **`택배비설정.txt`** 에서 바꿉니다. 메뉴 `6번`.

## 6. 보관박스 금액 규칙

주문 파일에 **보관박스 표시가 있는 행만** 금액이 들어갑니다. 표시가 없으면 빈칸입니다.

금액은 **짧은 변**으로 정합니다. **긴 변이 아닙니다.**

| 짧은 변 | 금액 |
|---|---|
| 254 미만 | **3,850원** |
| 254 이상 355 미만 | **4,400원** |
| 355 이상 | **7,700원** |

여기에 **수량을 곱한 값**이 M열에 들어갑니다.

```
152x203  수량 1  ->  3,850
203x305  수량 1  ->  3,850   (긴 변이 305라도 짧은 변이 203이라 작은 금액)
254x254  수량 1  ->  4,400
305x406  수량 1  ->  4,400
355x355  수량 1  ->  7,700
406x406  수량 2  -> 15,400   (7,700 x 2)
152x203  수량 3  -> 11,550   (3,850 x 3)
```

> 세 금액 모두 **부가세가 포함된 값**입니다 (3,500 · 4,000 · 7,000 + 10%).
> 그래서 합계 수식에서 VAT를 다시 곱하지 않고 그대로 더합니다.

### 금액·기준 바꾸기

메뉴 `4번` 을 고르면 메모장이 열립니다. 숫자만 고치고 저장하면 바로 적용됩니다.

```
기준1 = 254
기준2 = 355
작은금액 = 3850
중간금액 = 4400
큰금액 = 7700
수량곱하기 = 예
기존글자도변환 = 예
```

### 글자로 남아 있는 "포장가방" 바꾸기

정산내역서에 글자로 `포장가방` 이라고 남아 있는 칸은 메뉴 `3번` 으로 금액으로 바꿉니다.
그 행의 사이즈·수량을 보고 계산합니다.

- 이미 **숫자가 들어 있는 칸은 건드리지 않습니다.**
- 주문을 추가할 때도 매번 자동으로 같이 돌아갑니다.
  (원치 않으면 설정에서 `기존글자도변환 = 아니오`)

## 7. 안전장치

- **자동 백업** — `백업` 폴더에 실행 직전 상태를 날짜·시각을 붙여 남깁니다.
  잘못됐으면 그 파일을 꺼내 쓰면 됩니다.
  바뀐 내용이 없으면 백업을 남기지 않아서 폴더가 쓸데없이 쌓이지 않습니다.
- **중복 방지** — 이미 들어가 있는 주문일은 건너뜁니다.
  같은 날짜를 일부러 한 번 더 넣어야 하면 메뉴 `2번`.
- **모르면 비웁니다** — 단가표에 없는 규격, 수량 2개 이상인 택배비는 억지로 채우지 않고
  비고에 표시를 남깁니다.

## 8. 파일 구성

| 파일 | 설명 |
|---|---|
| `정산내역서 자동화.bat` | **이것만 쓰면 됩니다** (끌어다 놓기 / 더블클릭) |
| `settlement.ps1` | 실제 동작하는 코드 |
| `단가표.txt` | 품목·사이즈별 단가 (66개 규격) |
| `택배비설정.txt` | 택배비 구간·금액 |
| `보관박스_금액설정.txt` | 보관박스 기준·금액 |
| `정산내역서_경로.txt` | 마지막으로 쓴 정산내역서 위치 (자동 생성) |
| `백업\` | 실행 결과를 되돌릴 수 있는 백업 |

설치할 프로그램은 없습니다. 윈도우 + 엑셀만 있으면 됩니다.
